1. Create the index and queries:

   * `mkindex.sh`
   * `mkquery.sh`

2. Perform searches using the queries (against the index):

   * `search.sh`

3. Evaluate the search results

   * `evaluate.sh`

4. Convert the results from TREC files to CSV's

   * `consolidate.sh`
